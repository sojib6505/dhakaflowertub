# Development: PORT=5000. Render can provide its own PORT at runtime.
PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/dhakaflowertub
NODE_ENV=development
# Use a comma-separated list for local plus deployed frontend origins.
CLIENT_URL=https://dhakaflowertub.vercel.app
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-random-password
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=8h

# Production example:
# NODE_ENV=production
# CLIENT_URL=https://your-app.vercel.app
